(()=>{const board=document.querySelector('#corkboard'),form=document.querySelector('#noteForm');let firstPaint=true,nextIsFresh=false;function decorate(){const notes=[...board.querySelectorAll('.note')];if(!notes.length&&!document.querySelector('#search').value&&!board.querySelector('.empty-board'))board.insertAdjacentHTML('beforeend','<div class="empty-board"><span class="empty-pin">📌</span><strong>Tu próxima gran idea empieza aquí.</strong><small>Toca dos veces el corcho para pegarla.</small></div>');notes.forEach((note,index)=>{note.style.setProperty('--arrival-delay',`${index*75}ms`);if(firstPaint)note.classList.add('entering')});if(!firstPaint&&nextIsFresh&&notes.length)notes.at(-1).classList.add('fresh');nextIsFresh=false;firstPaint=false}new MutationObserver(()=>requestAnimationFrame(decorate)).observe(board,{childList:true,subtree:true});decorate();form.addEventListener('submit',event=>{if(event.submitter?.value==='save'&&document.querySelector('#noteText').value.trim())nextIsFresh=true},true);setTimeout(()=>{const notes=[...board.querySelectorAll('.note')];if(!notes.length||board.querySelector('.memory-note'))return;const text=notes[Math.floor(Math.random()*notes.length)].querySelector('.note-content').textContent.trim(),reminder=document.createElement('aside');reminder.className='memory-note';reminder.innerHTML=`<button aria-label="Cerrar">×</button><b>Una idea para recordar</b><span>${text.slice(0,88)}${text.length>88?'…':''}</span>`;reminder.querySelector('button').onclick=()=>reminder.remove();board.append(reminder)},1800)})();
/* Living Workspace v1.0.1 */
(()=>{const b=document.querySelector('#corkboard'),f=document.querySelector('#noteForm');let first=true,fresh=false;function living(){const notes=[...b.querySelectorAll('.note')];notes.forEach((n,i)=>{n.style.setProperty('--arrival-delay',`${i*75}ms`);if(first)n.classList.add('entering');if(!n.querySelector('.note-meta'))n.insertAdjacentHTML('beforeend',`<span class="note-meta">Creado · ${new Date().toLocaleDateString('es',{day:'numeric',month:'short'})}</span>`)});if(!notes.length&&!b.querySelector('.empty-board'))b.insertAdjacentHTML('beforeend','<div class="empty-board"><span class="empty-pin">📌</span><strong>Tu próxima gran idea empieza aquí.</strong><small>Toca el corcho para empezar.</small></div>');if(!first&&fresh&&notes.length)notes.at(-1).classList.add('fresh');fresh=false;first=false}new MutationObserver(()=>requestAnimationFrame(living)).observe(b,{childList:true,subtree:true});living();f.addEventListener('submit',e=>{if(e.submitter?.value==='save')fresh=true},true);b.addEventListener('dblclick',e=>e.stopImmediatePropagation(),true);b.addEventListener('click',e=>{if(e.target===b||e.target.classList.contains('empty-board')){const r=b.getBoundingClientRect();openEditor(null,{x:Math.min(81,Math.max(2,(e.clientX-r.left)/r.width*100)),y:Math.min(77,Math.max(2,(e.clientY-r.top)/r.height*100))})}},true);const h=document.createElement('button');h.className='side-action';h.innerHTML='<span>✦</span> Tu Historia';document.querySelector('.sidebar-bottom').prepend(h);h.onclick=()=>{const p=document.createElement('aside');p.className='memory-note';p.style.position='fixed';p.style.right='24px';p.style.top='100px';p.innerHTML=`<button>×</button><b>Tu Historia</b><span>📌 ${state.notes.length} ideas creadas<br>🔥 Cada idea cuenta.</span>`;p.querySelector('button').onclick=()=>p.remove();document.body.append(p)}})();
